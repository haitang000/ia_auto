name: CI

on:
  push:
    branches:
      - main
    tags:
      - "v*"
  pull_request:
    branches:
      - main

permissions:
  contents: write

jobs:
  build:
    name: Build
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "21"
          cache: maven

      - name: Package with Maven
        run: mvn --batch-mode package

      - name: Prepare plugin jar
        if: startsWith(github.ref, 'refs/tags/')
        run: |
          mkdir -p dist
          cp target/iaauto-*.jar dist/IAAuto.jar

      - name: Publish plugin jar
        if: startsWith(github.ref, 'refs/tags/')
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          if gh release view "$GITHUB_REF_NAME" >/dev/null 2>&1; then
            gh release upload "$GITHUB_REF_NAME" dist/IAAuto.jar --clobber
          else
            gh release create "$GITHUB_REF_NAME" dist/IAAuto.jar --title "$GITHUB_REF_NAME" --generate-notes
          fi
